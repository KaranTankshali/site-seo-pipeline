# Gemini / AI Studio — site-seo-pipeline

This repo implements **site-seo-pipeline** (Node.js ESM, TypeScript).

**Primary context:** [AGENTS.md](./AGENTS.md)  
**User guide:** [README.md](./README.md)

Before finishing a change: `npm run build` and `npm test`. Public API lives in `src/index.ts`; use `.js` suffixes in TypeScript imports under `src/`.
